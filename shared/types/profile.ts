enum Role {
    InterviewlyStaff = 'interviewlyStaff',
    Admin = 'admin',
    Moderator = 'moderator',
    Observer = 'observer',
};

enum Gender {
    MALE = 'male',
    FEMALE = 'female',
};

enum Nationality {
    Polish = 'polish',
    British = 'british',
    French = 'french',
};

enum Sector {
    Education = 'education',
};

export {
    Role,
    Gender,
    Nationality,
    Sector,
};