{
  "name": "shared",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "install": "yarn run build"
  },
  "author": "Daniel Grad",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.3"
  }
}
